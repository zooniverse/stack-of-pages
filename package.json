{
  "name": "stack-of-pages",
  "version": "0.1.5",
  "main": "./stack-of-pages.js",
  "devDependencies": {
    "coffee-script": "~1.7.1",
    "serveup": "0.0.5"
  },
  "scripts": {
    "compile": "coffee --compile --watch ./stack-of-pages.coffee",
    "serve": "serveup --port 4635",
    "start": "npm run compile & npm run serve"
  }
}
